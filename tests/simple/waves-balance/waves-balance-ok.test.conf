include required("./common/options.conf")

test.expected {
  available {
    type = Int
    value = 100
  }

  regular {
    type = Int
    value = 100
  }

  generating {
    type = Int
    value = 100
  }

  effective {
    type = Int
    value = 100
  }
}
